// if a class extend another class, then lombok will not include parent class elements in child class in constructor, so we need to create a constructor manually
//refer to User class


//entity package we are keeping POJOs , which related database

//List should come from DB, you shouldn't put list in controller class

//Usually when we restart app in company bootstrap needs to be innitiated first

////CommandLineRunner, springboot is providing it, whenever we start the application, this class executes first
//CommandLineRunner, springboot is providing it, whenever we start the application, this class executes first